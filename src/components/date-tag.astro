---
type Props = {
  date: string;
};

const { date } = Astro.props;
---

<a
  href={`/journal/date/${encodeURI(new Date(date).getFullYear().toString())}`}
  target="_self"
  class="meta-date"
>{date}</a>
